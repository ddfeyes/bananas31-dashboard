[pytest]
asyncio_mode = auto
testpaths = backend/tests tests
addopts = -m "not slow"
markers =
    integration: mark test as integration (requires live server at localhost:8765)
    slow: mark test as slow (requires docker restart, skipped by default)
